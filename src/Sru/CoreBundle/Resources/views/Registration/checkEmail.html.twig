{% extends "SruCoreBundle:FrontOffice:layout.html.twig" %}
{% trans_default_domain 'FOSUserBundle' %}

{% block content %}
    {{ parent() }}
    <div class="row large-12">
        <p class="alert-box success text-center">{{ 'registration.check_email'|trans({'%email%': user.email}) }}</p>
    </div>
{% endblock %}