<form method="post" class="text-left" role="form" action="{{ path('order_update', {id :entity.id}) }}" {{ form_enctype(form) }}>


    <button class="btn btn-warning">
        <span class="glyphicon glyphicon-ok"></span> &nbsp;
        Modifier la commande et envoyer l'email</button>

    <br><br>

    {% include "SruCoreBundle:BackOffice/OrderUser:form.content.html.twig" %}

</form>