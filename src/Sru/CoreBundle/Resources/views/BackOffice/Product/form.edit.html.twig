<form method="post" class="text-left" role="form" action="{{ path('admin_product_update', {id: entity.id}) }}" action-picture="{{ path('admin_product_pictures') }}" {{ form_enctype(form) }}>


<button class="btn btn-warning">
    <span class="glyphicon glyphicon-ok"></span> &nbsp;
    Modifier le produit</button>

<br><br>

    {% include "SruCoreBundle:BackOffice/Product:form.content.html.twig" %}

</form>